# Character Frequency Counter

Java program to count the frequency of each character in a user-input string.

## How it works
- Takes a string input from the user
- Uses nested loops to count occurrences of each character
- Marks already-counted characters to avoid duplicate counting
- Prints each unique character with its frequency

## Run
javac CharacterFrequencyCounter.java
java CharacterFrequencyCounter