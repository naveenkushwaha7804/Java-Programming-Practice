# Substring Search

Java program to find whether a substring exists within a string, and return its starting index.

## How it works
- Takes a main string and a substring as input
- Uses `String.indexOf()` to find the position of the substring
- If found, prints the starting index; otherwise prints -1

## Run
javac SubstringSearch.java
java SubstringSearch